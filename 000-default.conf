<VirtualHost *:80>
    ServerName comamorevontade.site
    ServerAlias www.comamorevontade.site

    # Redirect all HTTP requests to HTTPS
    Redirect permanent / https://comamorevontade.site/

    # (Optional) If you want to preserve www → non-www or vice-versa, adjust accordingly
    # Example: for www → non-www you could add:
    # RewriteEngine On
    # RewriteCond %{HTTP_HOST} ^www\.comamorevontade\.site$ [NC]
    # RewriteRule ^/?(.*) https://comamorevontade.site/$1 [L,R=301]
</VirtualHost>
